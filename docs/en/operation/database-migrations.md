# Database Migrations

This chapter explains how database migrations are managed.

## Alembic 

```plaintext
├── Makefile
└── backend
    ├── alembic
    │   ├── versions
    |   ├── env.py
    |   └── script.py.mako
    ├── oqtopus-dev
    └── alembic.ini
```

(Those files unrelated to the db migrations are omitted)

`backend/versions` directory contains a bunch of migration files.
In the each migration file, specify the 
In many case, the modfications are automatically generated by running `make mig`  
`script.py.mako` is a template for migration file.

Suppose you have following table definition in terms of sqlalchemy data model:

```py
```

With this placed in the path `backend/oqtopus_cloud/common/models/`, running the command

```sh
make mig-gen ARG="create_jobs_table"
```

will generate the following migration script:

```py

```
