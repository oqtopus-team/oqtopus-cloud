mfa_reset.MfaResetStartRequest:
  type: object
  required:
    - email
    - password
  properties:
    email:
      type: string
      format: email
    password:
      type: string

mfa_reset.MfaResetStartResponse:
  type: object
  properties:
    access_token:
      type: string
      description: Access token to access Cognito API

mfa_reset.MfaResetVerifyCodeRequest:
  type: object
  required:
    - access_token
    - code
  properties:
    access_token:
      type: string
    code:
      type: string

mfa_reset.MfaResetVerifyCodeResponse:
  type: object
  properties:
    secret:
      type: string
      description: Base32 encoded secret for TOTP

mfa_reset.MfaResetConfirmTotpRequest:
  type: object
  required:
    - access_token
    - totp_code
  properties:
    access_token:
      type: string
    totp_code:
      type: string
